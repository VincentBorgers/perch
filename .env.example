# Random string used to sign the admin session cookie.
# The setup script creates one for you if this is empty.
SESSION_SECRET=

# Optional. Folder that holds db.json. Defaults to ./data
# PERCH_DATA_DIR=

# Optional. Port for npm start. Defaults to 3000
# PORT=3000
